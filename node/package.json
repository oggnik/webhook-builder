{
  "name": "webhook-builder",
  "version": "0.0.0",
  "description": "A simple server that executes a script when a webhook is called.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/oggnik/webhook-builder.git"
  },
  "author": "Nikolas Ogg",
  "bugs": {
    "url": "https://github.com/oggnik/webhook-builder/issues"
  },
  "dependencies": {
    "express": "~3.4.4"
  }
}
